[gd_scene format=3 uid="uid://bsknvhwny5hb0"]

[sub_resource type="GDScript" id="GDScript_r3tlg"]
script/source = "extends CharacterBody3D


const SPEED = 5.0
const JUMP_VELOCITY = 4.5


func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var input_dir := Input.get_vector(\"Move Left\", \"Move Right\", \"Move Down\", \"Move Up\")
	var direction := (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()
	if direction:
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		velocity.z = move_toward(velocity.z, 0, SPEED)

	move_and_slide()
"

[sub_resource type="SphereShape3D" id="SphereShape3D_r3tlg"]

[sub_resource type="CapsuleMesh" id="CapsuleMesh_4c035"]
radius = 0.49560547
height = 0.99121094

[node name="CharacterBody3D" type="CharacterBody3D" unique_id=1851737853]
script = SubResource("GDScript_r3tlg")

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=1633357800]
shape = SubResource("SphereShape3D_r3tlg")

[node name="MeshInstance3D" type="MeshInstance3D" parent="." unique_id=2090701082]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.0014648438, 0)
mesh = SubResource("CapsuleMesh_4c035")
